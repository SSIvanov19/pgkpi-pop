# Арменският Поп — Форма за оплаквания

Уеб приложение, което позволява на потребителите да споделят своите оплаквания. Данните се записват в Google Sheets чрез Google Apps Script.

---

## Бързо стартиране

### 1. Създаване на Google Sheet

1. Отвори [Google Sheets](https://sheets.google.com) и създай **нова** таблица
2. Преименувай я на **„Арменският Поп — Оплаквания"** (или каквото предпочиташ)

### 2. Добавяне на Apps Script

1. В Google Sheet отвори **Разширения → Apps Script**
2. Изтрий целия код в редактора
3. Копирай съдържанието на файла `google-apps-script.js` от този проект и го постави в редактора
4. Натисни **💾 Save** (Ctrl+S)
5. *(По избор)* Изпълни функцията `setupSheet` веднъж, за да добави заглавен ред:
   - От падащото меню за функции избери `setupSheet`
   - Натисни **▶ Run**
   - Одобри разрешенията, когато бъдеш попитан

### 3. Деплой на Apps Script като Web App

1. В Apps Script редактора натисни **Deploy → New deployment**
2. Кликни иконката ⚙ до „Select type" и избери **Web app**
3. Попълни:
   - **Description**: `Complaint form handler`
   - **Execute as**: `Me`
   - **Who has access**: `Anyone`
4. Натисни **Deploy**
5. **Копирай URL-а** на Web App (изглежда така: `https://script.google.com/macros/s/XXXX/exec`)

### 4. Конфигуриране на приложението

1. Отвори `index.html` в текстов редактор
2. Намери реда:
   ```js
   const GOOGLE_SCRIPT_URL = 'YOUR_GOOGLE_APPS_SCRIPT_WEB_APP_URL';
   ```
3. Замени `YOUR_GOOGLE_APPS_SCRIPT_WEB_APP_URL` с URL-а, който копира в стъпка 3.5
4. Запази файла

### 5. Отваряне

Отвори `index.html` в браузър — готово! Можеш също да го хостнеш на GitHub Pages, Netlify и др.

---

## Структура на проекта

```
ArmenskiPop/
├── index.html              ← Клиентското приложение (HTML + CSS + JS)
├── google-apps-script.js   ← Код за Google Apps Script (копира се в Sheet-а)
└── README.md               ← Това файл
```

## Как работи

```
Потребител попълва формата
        │
        ▼
  fetch() POST → Google Apps Script Web App
                        │
                        ▼
               Добавя ред в Google Sheet
                  (Дата | Име | Оплакване)
```

---

## Отстраняване на проблеми

| Проблем | Решение |
|---------|---------|
| Вижда се „Приложението не е конфигурирано" | Замени `YOUR_GOOGLE_APPS_SCRIPT_WEB_APP_URL` с реалния URL |
| Формата се изпраща, но Sheet-ът е празен | Провери дали Apps Script е деплойнат с „Anyone" достъп |
| Грешка „Authorization required" | В Apps Script изпълни `doPost` ръчно веднъж и одобри разрешенията |
| Промени в Apps Script не се отразяват | Създай **нов deployment** (New deployment), не ъпдейтвай стария |
